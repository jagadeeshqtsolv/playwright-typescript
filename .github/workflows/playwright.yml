name: Playwright Tests
on:
  push:
    branches: [ blazedemo ]
  pull_request:
    branches: [ blazedemo ]
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: 18
    - name: Install dependencies
      run: npm ci
    - name: Install Playwright Browsers
      run: npx playwright install --with-deps
    - name: Run Playwright tests
      run: npm run web
      continue-on-error: true
    - name: Allure Report Generation
      run: npm run alluregenerate
      continue-on-error: true
    - name: Upload a Build Artifact
      uses: actions/upload-artifact@v3.1.1
      with:
          name: Report
          path: ${{ github.workspace }}/allure-report/

    - name: Send mail
      uses: dawidd6/action-send-mail@v3
      with:
          server_address: smtp.gmail.com
          server_port: 465
          username: feautomationuser@gmail.com
          password: ceeqdbpbodukvahm
          subject:  Test Automation Suite Report
          to: jagadeesh@qtsolv.com
          from: Automation Team<jagadeesh@qtsolv.com>
          secure: true
          attachments: ${{github.workspace}}/allure-report/index.html
